files(
  unique int id: @file,
  string path: string ref
);

functions(
  unique int id: @function,
  int file: @file ref,
  string name: string ref,
  int start_line: int ref,
  int end_line: int ref
);

string_literals(
  unique int id: @string_literal,
  int file: @file ref,
  string value: string ref,
  int line: int ref
);

calls(
  unique int id: @call,
  int caller: @function ref,
  int callee: @function ref,
  int file: @file ref,
  int line: int ref
);
